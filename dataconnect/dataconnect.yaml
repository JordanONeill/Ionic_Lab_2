specVersion: "v1"
serviceId: "movie-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "movie-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
